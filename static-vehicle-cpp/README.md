# Static-Vehicle-CPP

A small C++ applicatio which creates a vehicle document in Atlas. Currently only kept as sample with no particular use. The vehicle-ts / vehicle simulator already has functionlity to create an entry if one was not created previously.

